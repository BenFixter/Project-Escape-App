import * as React from 'react';

import MaterialIcons from 'react-native-vector-icons/MaterialIcons';

function BackButton() {
  return <MaterialIcons name="arrow-back" color="#fff" size={26} />;
}

export default BackButton;
